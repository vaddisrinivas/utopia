Build a tiny interval timer for study, chores, cooking, and workouts.

The app should store named presets, duration, rest length, completed sessions,
and notes. Use a widget-backed timer/control surface plus a history screen.
It should work as a local-first utility, not just a record table.
